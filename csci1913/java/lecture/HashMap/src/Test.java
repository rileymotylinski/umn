public class Test {

    // Question: reorganizing bst
    // - putting sorted array -> bst

    // hash tables of O(1) for everything
    // - add, search, remove
    // hash tables implement maps
    // a map just puts key -> value
    // a hash turns a value to a key and vice versa
    // use a value to calculate a key

    // to avoid collisions
    // - how big should I table be? (should be prime number length because of divisions, etc.)


    /*
    Minimal perfect Hash Function
    hash - if x == y, then h(x) = H(y)
    perfect - if h(x) == h(y), then x == y
    minimal - only uses numbbers 0 to n - 1
     */
}
